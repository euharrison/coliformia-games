machine:
  node:
    version: "4"

dependencies:
  pre:
    - sudo pip install awsebcli

deployment:
  production:
    branch: auto-deploy
    commands:
      - bash ./deploy/setup-eb.sh
      - eb deploy