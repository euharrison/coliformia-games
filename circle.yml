machine:
  node:
    version: "4"

deployment:
  staging:
    branch: easy-setup
    commands:
      - git checkout gh-pages
      - git merge master
      - git add .
      - git commit -m "circleci merge"
      - git push