machine:
  node:
    version: "4"

dependencies:
  pre:
    - pip install awsebcli

deployment:
  staging:
    branch: auto-deploy
    commands:
      - npm run build
      - eb init
      - eb deploy