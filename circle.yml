machine:
  node:
    version: "4"

dependencies:
  pre:
    - pip install awscli

deployment:
  staging:
    branch: auto-deploy
    commands:
      - aws elasticbeanstalk update-environment --environment-name cg-app --version-label v1